{
  "name": "ts-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf artifacts cache node_modules typechain-types",
    "hardhat:compile": "npx hardhat compile",
    "hardhat:test": "npx hardhat run scripts/buy-coffee.ts",
    "hardhat:deploy": "npx hardhat run scripts/deploy.ts",
    "hardhat:publish": "npx hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0"
  }
}
